// because thrust reversing is just a specific use case of ModuleAnimateGeneric, we need this PartModule to identify engines that can have their thrust reversed
// These are the only two in stock, but other mods should add this module to their own engines if they support reversing thrust
@PART[turboFanSize2|JetEngine]
{
    %MODULE[JSIThrustReverser] {}
}
